

class ReactBuilderMain {
  render (props) {
    <div/>
  }
}


ReactBuilderMain.propTypes = {
  required: React.PropTypes.bool,
};

const myComponent = {
  initialState(), // returns a state object
  reactBuilderMain() // returns a react class
}

export default myComponent
